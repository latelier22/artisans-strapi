{
   
    "builds": [
      {
        "src": "next.config.js",
        "use": "@vercel/next",
        "config": {
          "installCommand": "pnpm install",
          "buildCommand": "npm run prebuild && npm run build"
        }
      }
    ]
  }